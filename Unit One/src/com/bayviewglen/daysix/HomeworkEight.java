package com.bayviewglen.daysix;

public class HomeworkEight {

	public static void main(String[] args) {
		double num, square, root;
		num = 9;
		square = num * num;
		root = Math.sqrt(num);
		System.out.println ("For the number "+ (int)num + ", the square is "+ (int)square +" and the root is " + (int)root);

	}

}
